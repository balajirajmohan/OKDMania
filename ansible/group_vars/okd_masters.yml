---
# OKD master / control-plane host vars.
#
# NOTE: a plain Fedora VM is NOT an OKD 4.x control plane. See
# docs/sandbox-findings.md. These vars are placeholders for Phase 2 OS prep
# (chrony, kernel params, DNS) and for whichever install path the team picks
# for the real cluster (UPI - see root README D031).

node_role: master
okd_required_ports_tcp: [6443, 22623, 2379, 2380, 10257, 10259]
